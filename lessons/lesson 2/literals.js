exports.FirstLiterals = () => {
    let year = 2001; //. The digits 1990, written in the code at the place of variable initialization, are a literal that represents a number.
    console.log(year);
    console.log(1999);
    console.log("Alice");

    // on the console the value 1991 and "Alice", in both cases using literals (representing a number and a string respectively). 
};


